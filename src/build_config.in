#ifndef BUILD_CONFIG_HPP
#define BUILD_CONFIG_HPP

inline constexpr auto VERSION_MAJOR = @rantanplan_VERSION_MAJOR@;
inline constexpr auto VERSION_MINOR = @rantanplan_VERSION_MINOR@;

#ifdef DEBUG_BUILD
inline constexpr bool DEBUG_MODE = true;
#else
inline constexpr bool DEBUG_MODE = false;
#endif

#ifdef DEBUG_LOG
inline constexpr bool DEBUG_LOG_ACTIVE = true;
#else
inline constexpr bool DEBUG_LOG_ACTIVE = false;
#endif

#ifdef PARALLEL
inline constexpr bool PARALLEL_MODE = true;
#else
inline constexpr bool PARALLEL_MODE = false;
#endif

#endif /* end of include guard: BUILD_CONFIG_HPP */
